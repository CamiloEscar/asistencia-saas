-- Standalone copy of the RLS policies (kept in sync with the migration file).
-- Use this for reference / for setting up RLS manually in a non-Prisma workflow.
--
-- Source of truth: prisma/migrations/20260616000000_init/migration.sql
-- Doc: engram `sdd/attendance-mvp/design` (#186) §4.4.

-- (See migration.sql for the full DDL. This file is documentation only;
--  applying it on top of a partially-migrated database will fail with
--  "policy already exists" errors. Always re-apply via `prisma migrate deploy`.)
