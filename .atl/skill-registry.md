# Skill Registry

> Auto-regenerated by `sdd-init` on 2026-06-18.
> This file is infrastructure, NOT an SDD artifact. The orchestrator reads it to resolve
> skill paths once per session, then passes pre-resolved paths to sub-agents.

## How to use

When launching a sub-agent that needs a specific skill, the orchestrator MUST pass the
absolute file path to the skill's `SKILL.md` in the prompt, e.g.:

```
SKILL: Load `C:\Users\camil\.claude\skills\sdd-spec\SKILL.md` before starting.
```

Sub-agents do NOT search for the skill registry themselves. They receive the path inline.

## Resolution rules

1. Scan order for user-level skills (first match wins):
   - `~/.claude/skills/`
   - `~/.config/opencode/skills/`
   - `~/.gemini/skills/`
   - `~/.cursor/skills/`
   - `~/.copilot/skills/`
   - Parent of the running skill file
2. Project-level skills (`.claude/skills/`, `.gemini/skills/`, `.agent/skills/`, `skills/`)
   override user-level skills with the same name.
3. `_shared` directories are excluded (convention storage, not skills).
4. All paths below were resolved by walking the scan order and recording the first
   physical file found. They are verified to exist on disk at init time.

## Available Skills (user-level)

| Skill           | Path                                                   | Trigger                                                            |
| --------------- | ------------------------------------------------------ | ------------------------------------------------------------------ |
| `go-testing`    | `C:\Users\camil\.claude\skills\go-testing\SKILL.md`    | Go tests, Bubbletea TUI testing                                    |
| `sdd-apply`     | `C:\Users\camil\.claude\skills\sdd-apply\SKILL.md`     | Implement tasks from a change, writing actual code                 |
| `sdd-archive`   | `C:\Users\camil\.claude\skills\sdd-archive\SKILL.md`   | Sync delta specs to main specs and archive a completed change      |
| `sdd-design`    | `C:\Users\camil\.claude\skills\sdd-design\SKILL.md`    | Create technical design document with architecture decisions       |
| `sdd-explore`   | `C:\Users\camil\.claude\skills\sdd-explore\SKILL.md`   | Explore and investigate ideas before committing to a change        |
| `sdd-init`      | `C:\Users\camil\.claude\skills\sdd-init\SKILL.md`      | Initialize Spec-Driven Development context in a project            |
| `sdd-propose`   | `C:\Users\camil\.claude\skills\sdd-propose\SKILL.md`   | Create a change proposal with intent, scope, and approach          |
| `sdd-spec`      | `C:\Users\camil\.claude\skills\sdd-spec\SKILL.md`      | Write specifications with requirements and scenarios (delta specs) |
| `sdd-tasks`     | `C:\Users\camil\.claude\skills\sdd-tasks\SKILL.md`     | Break down a change into an implementation task checklist          |
| `sdd-verify`    | `C:\Users\camil\.claude\skills\sdd-verify\SKILL.md`    | Validate that implementation matches specs, design, and tasks      |
| `skill-creator` | `C:\Users\camil\.claude\skills\skill-creator\SKILL.md` | Creates new AI agent skills following the Agent Skills spec        |

## Project-level skills

None. The following locations were scanned and empty:

- `C:\Users\camil\Proyectos\asistencia-saas\.claude\skills\`
- `C:\Users\camil\Proyectos\asistencia-saas\.gemini\skills\`
- `C:\Users\camil\Proyectos\asistencia-saas\.agent\skills\`
- `C:\Users\camil\Proyectos\asistencia-saas\skills\`

## Customization Skill

| Skill                | Path         | Trigger                                                         |
| -------------------- | ------------ | --------------------------------------------------------------- |
| `customize-opencode` | `<built-in>` | Editing opencode config, agents, subagents, skills, MCP servers |

## Convention files scanned

- `C:\Users\camil\.config\opencode\AGENTS.md` (global rules)
- No project-level `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `.cursorrules`, or `copilot-instructions.md` were found in the project root.

## Notes

- This registry is mode-independent. It exists regardless of whether the active SDD
  persistence backend is `engram`, `openspec`, `hybrid`, or `none`.
- The `customize-opencode` skill is provided by opencode itself and resolves at runtime.
- Scan the project root and conventional locations (`.claude/skills/`, `.agent/skills/`,
  `skills/`) before launching sub-agents in a new project — project-level skills override
  user-level ones.
- The same skill file may physically exist in multiple user-level directories (e.g.
  `~/.claude/skills/...` and `~/.config/opencode/skills/...` are independent copies).
  This registry records ONE canonical path per skill — the first match in scan order.
